spring:
  application:
    name: ms-template-jdbc
  datasource:
    url: jdbc:mysql://localhost:3306/microservice_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  logging:
    level:
      root: DEBUG # Enable debug-level logging